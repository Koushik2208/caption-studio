# PROGRESS.md

## CURRENT STATE (update after every completed step)
Verification backlog (items 1-5 below) is CLEARED — all confirmed live by the user, including real export checks for position, Cinematic Scope inset, and texture overlays. G4 (frame+overlay+caption compositing, live AND export) is satisfied by that same pass.

Next action: Part H (Reel Craft ports + transcript editing) is **fully done and verified**. H5 confirmed via a real 4-minute Chroma Key export that reflected the user's transcript edits correctly. Also confirmed during H5 checking: the Chroma Key export intentionally has no audio track (captions-only overlay meant to be composited over real footage in an editor) — this is by design, not a bug, and matches the already-logged "Video/MP4-with-footage export — deferred" gap.

Session 6 (PLAN.md Part I) — orientation-aware fonts + global legibility — **fully done and verified**. I1-I4 all complete, live-verified by the user including frames/textures at both orientations.

Session 7 (Part G continued) — 3 more frames ported from Reel Craft: Film Strip, Square Bezel, Vintage Projector — **fully done and verified**. Wired into `src/frames/types.ts` (`FrameVariant`), `FrameRenderer.tsx`, `overlayOptions.ts` (`FRAME_OPTIONS`), `OverlayFramesPage.tsx` (shell-color picker now also shows for Square Bezel, matching Minimal Bezel). `contentInset.ts` gained a `compositionWidth` param and a Film Strip case (its opaque sprocket rails cover left/right edges like Cinematic Scope's bars cover top/bottom) — caught and fixed a real bug in the process: `CaptionPreviewComposition`/`CaptionExportComposition`'s `hasFrameInset` z-index gate only checked `top`/`bottom`, so a left/right-only inset would've silently failed to elevate captions above the rails; now checks all 4 edges. Square Bezel/Vintage Projector need zero inset (Square Bezel shrinks the children viewport like Minimal Bezel does; Vintage Projector's vignette is soft, not opaque, like Gradient Border/Neon Glow). `tsc`/`build` clean, verified via a real `bundle()` → `selectComposition()` → `renderStill()` pass through the actual export composition for all 3 variants, and **live-verified by the user in the browser** (Overlay → Frame tab).

Session 8 (Part G continued) — 3 more texture overlays ported from Reel Craft: CRT Scanlines, Halftone, Light Leak. New files `src/textures/CrtScanlines.tsx`/`Halftone.tsx`/`LightLeak.tsx` (dimensions rebased onto `useVideoConfig()` instead of reel-craft's fixed WIDTH/HEIGHT constants, same adaptation as the existing 3 textures). `TextureOverlaySettings` (`src/textures/types.ts`) gained 3 enabled+intensity pairs; `TextureOverlayRenderer.tsx` stacks them (grid → halftone → film dust → light leak → CRT scanlines → halation — subtlest/structural first, glow last; this ordering is an editorial choice made during the port, not a documented reel-craft spec). `ProjectContext.tsx` got matching state/setters/persistence/provider-value plumbing (mirrors the halation/grid pattern exactly). `OverlayTextureOverlaysPage.tsx` gained toggle+intensity UI blocks for all 3 (condensed Overlay-tab card unchanged — it only ever surfaced Film Dust, same as before). `tsc`/`build` clean, verified via a real `bundle()` → `selectComposition()` → `renderStill()` pass through the actual export composition for all 3 (scanline pattern + moving bright bar, halftone dots, warm light-leak blob all confirmed rendering correctly). Not yet live-verified in the browser (Overlay → Texture Overlays tab).

Session 9 (Part G continued) — 3 motion graphics ported from Reel Craft: Code Block, Number Counter, Ticker. New `src/motion/` directory (first motion graphic, Progress Bar, lived in `src/overlay/` alongside Watermark — this port introduces the dedicated directory since there are now 4 total and more growth is expected): `types.ts` (`MotionGraphicsSettings`, independently-toggleable like textures, not a variant switch like frames), `CodeBlock.tsx`/`NumberCounter.tsx`/`Ticker.tsx` (flat props per component, matching the existing `ProgressBarOverlay`/`WatermarkOverlay` convention rather than reel-craft's config-object props), `MotionGraphicsRenderer.tsx` (stacks all 3, mirrors `TextureOverlayRenderer.tsx`), `CodeEditorModal.tsx`. Ticker's scroll-loop math was rebased from reel-craft's fixed vertical-only `WIDTH` constant onto `useVideoConfig().width` (same class of fix as Session 8's textures); its separator string also had corrupted unicode in the pasted source, rewritten clean. `ProjectContext.tsx` got the full 6-touchpoint pattern (persisted state, interface, useState×14, derived `motionSettings` memo, writeToStorage, provider value) plus a `motionSettings` prop threaded through `CaptionPreviewComposition`/`CaptionExportComposition`/`PreviewPlayer`/`Root.tsx`/`ExportPage.tsx`/`server/index.ts` (mirrors `textureSettings`'s plumbing exactly). Renders inside the same `hasFrameInset`-gated layer as Watermark/Progress Bar, after them, so motion graphics paint above captions.

UX decision on Code Block specifically: its code/language/position/linesPerPage fields are all edited together in `CodeEditorModal` (opened via an "Edit Code" button in the Overlay-tab card, which itself only shows a toggle + one-line summary) rather than any fields living inline in the panel — the code textarea needs real room that the narrow tool panel can't give it, and reel-craft's own source already bundled all 4 fields into one modal+Save flow, so that design was kept rather than fragmented. `LayoutContext.tsx` gained `isCodeEditorOpen` (same one-flag-flips-a-root-level-modal pattern as `isTranscriptEditorOpen`); `CodeEditorModal` is split into an always-mounted outer shell + a content component that only mounts while open, so its local draft state initializes fresh from `ProjectContext` on every open instead of going stale after the first open/close cycle (a real bug that would've hit if it followed `SettingsModal`/`TranscriptEditorModal`'s single-component shape verbatim, since neither of those has local draft state to go stale). `tsc`/`build` clean, verified via a real `bundle()` → `selectComposition()` → `renderStill()` pass through the actual export composition for all 3 (syntax-highlighted paginated code panel with page counter, interpolated `$500K`-style counter with prefix/suffix, and scrolling ticker text with the corrected separator all confirmed rendering correctly). Not yet live-verified in the browser (Overlay tab: Number Counter/Ticker/Code Block cards, plus the Edit Code modal).

Session 10 (Part E's deferred MP4-with-footage export) — **fully done and live-verified by the user in the browser**. New `src/preview/CaptionExportVideoComposition.tsx` — same layer stack as `CaptionPreviewComposition` (video + captions + frame/texture/motion overlays) but `<OffthreadVideo>` instead of `<Video>`, since this renders server-side via `renderMedia()` rather than in the browser `<Player>`. Registered as a second `<Composition id="CaptionExportVideo">` in `Root.tsx` (both compositions now render as siblings inside the single registered `RemotionRoot` — first attempt wrongly put the new one in a separate, never-registered root component; caught before verification). `server/index.ts` gained a multipart `POST /api/export-video` (multer `videoUpload`, extension-preserving filenames, separate from the transcribe endpoint's `upload`) plus the same job/poll/download pattern as Green Screen — factored the now-duplicated status/download route pair into a `registerJobRoutes(prefix)` helper shared by both formats. `ExportPage.tsx`'s "Video (MP4)" option is enabled only when `mediaFile` exists **and** is a real video (`mediaFile.type.startsWith('video/')`) — per user decision, disabled (with an explanatory note) both when media was lost to a page reload (mediaFile only lives in memory, unlike the localStorage-cached transcript) and for audio-only sources, which stay routed to Chroma Key/SRT instead; an effect resets `selectedFormat` away from `'mp4'` if it becomes unavailable mid-selection. Factored `exportGreenScreen`/new `exportVideo`'s near-identical poll+download loop into a shared `trackRenderJob(startResponse, endpointBase)`.

**Risk item (flagged in the plan, explicitly verified before calling this done):** assumed a local absolute file path would work directly as `OffthreadVideo`'s `src` for server-side rendering. A real `bundle()` → `selectComposition()` → `renderMedia()` pass against a synthetic test video proved this wrong — Remotion's asset downloader hard-rejects non-`http(s)` URLs even server-side (`Error: Can only download URLs starting with http:// or https://`). Fixed by having `server/index.ts` serve `UPLOAD_DIR` itself (`app.use("/tmp-media", express.static(...))`, scoped to exactly that directory) and passing an `http://localhost:${PORT}/tmp-media/<filename>` URL instead of the raw path. Sanity-checked the static route: `../` traversal attempts 404, no directory listing, nothing outside `/tmp-media` is served. Re-ran the same verification pass against the real running dev server with the HTTP URL — succeeded end-to-end; confirmed via `ffmpeg -i` probe that the output has real h264 video + aac audio streams (audio muxed automatically, no extra `<Audio>` needed) at the correct ~4s duration, and via a rendered still frame that actual source-video content is composited in (cropped via `objectFit: cover`), not a blank/black frame. Full LEARNINGS.md entry added — this local-path restriction applies to any future dynamically-provided asset fed into `OffthreadVideo`/`Video`/`Audio` (subject images, b-roll), not just this feature. `tsc -b` and `npm run build` both clean throughout.

Session 11 (Part G continued) — Chromatic Aberration added as a 4th texture overlay, not a reel-craft port (built new, since it needs a fundamentally different compositing approach from Film Dust/Halation/Grid/CRT Scanlines/Halftone/Light Leak) — **fully done and live-verified by the user in the browser, including a real export**. New `src/textures/ChromaticAberration.tsx`: unlike the other 5 textures (which paint additively on top via `TextureOverlayRenderer`), this **wraps** the video/background layer itself in an SVG `<filter>` — `feColorMatrix` isolates R/G/B channels onto separate black-elsewhere layers, `feOffset` shifts the red/blue ones in opposite horizontal directions (1px/2px/4px for low/medium/high), and `feBlend mode="screen"` recombines them (not `feMerge` — see LEARNINGS.md, `feMerge`'s normal over-compositing would let the topmost channel layer fully occlude the others instead of adding them back together). Wired into `CaptionPreviewComposition.tsx`/`CaptionExportVideoComposition.tsx` by wrapping just the `<Video>`/`<OffthreadVideo>` element, and into `CaptionExportComposition.tsx` by wrapping the chroma-green `AbsoluteFill` background fill (added a nesting level there since that composition has no real media element) — in all three, applied *before* `TextureOverlayRenderer`/captions/frame chrome in render order, so only the underlying media/background gets the color-fringing effect. `TextureOverlaySettings` gained `chromaticAberrationEnabled`/`chromaticAberrationIntensity`; `ProjectContext.tsx` got the full persisted-state/interface/useState/memo/writeToStorage/provider-value plumbing (mirrors halation/grid exactly). `OverlayTextureOverlaysPage.tsx` gained a toggle+intensity block (condensed Overlay-tab card unchanged, same as the other 5 textures — it only ever surfaces Film Dust). `tsc`/`build` clean, verified via a real `bundle()` → `selectComposition()` → `renderStill()` pass against a temporary high-contrast test composition (deleted after verification, not committed): confirmed actual red/cyan fringing at contrast edges, and confirmed a sibling rendered after the wrapped layer (standing in for captions) shows zero fringing.

Session 12 (Part G continued) — 3 new effects, not reel-craft ports (built new, per explicit user request): Audio-Reactive Pulse, Keyword-Synced Punch, True Film Grain — **Film Grain and Audio-Reactive Pulse live-verified by the user in the browser; Keyword-Synced Punch still only verified via the synthetic renderStill() pass below**. Details:
- **Film Grain** (`src/textures/FilmGrain.tsx`): additive overlay like the other 6 textures - `feTurbulence` seeded by `useCurrentFrame()` (not `Math.random()`) for deterministic per-frame noise, `mixBlendMode: 'overlay'` at low opacity. Distinct from Film Dust's discrete moving specks. Wired into `TextureOverlayRenderer.tsx`.
- **Audio-Reactive Pulse** (`src/textures/AudioPulse.tsx`): wraps the video/background layer (like Chromatic Aberration) in a `scale`+`brightness` transform driven by per-frame RMS amplitude, smoothed with a ±4-frame rolling average. The amplitude data itself is computed **server-side** during transcription (`server/index.ts`'s `computeAudioAmplitude` - a minimal hand-rolled RIFF/WAVE parser reading the same 16kHz mono WAV whisper.cpp already transcribes, no new npm dependency, no `@remotion/media-utils` needed since this sidesteps the local-file-path/http(s)-URL restriction entirely by working directly against the on-disk file before cleanup), peak-normalized to 0-1, returned as `audioAmplitude` alongside `captions` from `/api/transcribe`. Cached in localStorage (`caption-studio:cached-audio-amplitude`, mirrors the existing cached-transcript pattern) and threaded through `ProjectContext` → `PreviewPlayer`/`ExportPage` → all 3 composition files → `server/index.ts`'s export inputProps, the same 6-touchpoint plumbing pattern as every other setting. Live-verification round-trip: added a temporary per-frame `console.log` + on-screen debug overlay to both Film Grain and Audio Pulse (removed once confirmed) so the user could see the actual seed/amplitude numbers moving frame-to-frame, not just guess from the visual result - confirmed real varying data in both, but the original spec'd "subtle 1.0-1.03" scale range was too subtle to see even with a song's dynamic range. Bumped `SCALE_RANGE`/`BRIGHTNESS_RANGE` up (`low`/`medium`/`high` now 0.03/0.07/0.12 scale and 0.08/0.18/0.3 brightness, roughly 3-4x the original) at the user's explicit request for visibility; live-confirmed working after the bump.
- **Keyword-Synced Punch** (`src/textures/KeywordPunch.tsx`): also wraps the video/background layer, in a brief (8-frame) `scale`+`blur`+`brightness` punch with a sine envelope (0→peak→0), timed exactly to each keyword token's `fromMs` via the same `isKeywordToken`/`processCaptions` the caption styles already use. No new data pipeline needed - reads `captions`+`styleOverrides.keywords`/`keywordHighlightEnabled`, already passed to every composition.
- **`src/textures/BackgroundEffectsRenderer.tsx`**: new composing wrapper (`AudioPulse` → `KeywordPunch` → `ChromaticAberration` → children) - replaces each composition file's standalone `<ChromaticAberration>` wrap around the video/background element, so the 3 wrap-the-layer effects are wired once instead of duplicated 3x across `CaptionPreviewComposition`/`CaptionExportComposition`/`CaptionExportVideoComposition`.
- Verification: real `bundle()`→`selectComposition()`→`renderStill()` pass (not committed, ran from a gitignored `tmp/` script per the established pattern for scripts that need the project's own `node_modules`) with `ffmpeg`-sampled pixel values confirming actual effect: Film Grain shows visible noise vs. flat color when off; Audio Pulse's background brightens `[0,178,64]`→`[0,195,70]` (quiet→loud amplitude, high intensity); Keyword Punch stays at base color off-window but flashes to `[0,209,76]` exactly at a keyword token's punch-envelope peak frame. `tsc -b`/`npm run build` both clean.
- Film Grain and Audio-Reactive Pulse **live-verified by the user in the browser** with a real uploaded/transcribed video (Audio-Reactive Pulse tested against a real song for its dynamic range). Keyword-Synced Punch is still only verified via the synthetic `renderStill()` pass above, not live in the browser, and none of the 3 have been checked via a real end-to-end export yet (only live Player preview).

Next: live-verify Keyword-Synced Punch in the browser (needs Keyword Highlight enabled on the Style tab with a keyword actually present in the transcript), then a real end-to-end export check for all 3. After that, decide further scope: (a) more Part G frames/overlays/motion graphics (source `reel-craft` repo has 18 frames/10 texture overlays/6 motion graphics total; now ported/built: 7 frames, 8 texture overlays + 2 new background effects, 4 motion graphics); (b) still-open Session 10 item: live-verify MP4 export in the browser at both orientations if time allows; (c) anything else.

Blockers: none.

## Session 13 — Minimal Bezel corner radius control (small feature, not a PLAN.md phase)
Minimal Bezel's corner radius was a fixed `contentWidth * 0.18` in `MinimalBezel.tsx` — exposed as a real adjustable setting, same pattern as `fontSizeMultiplier`:
- New `src/frames/bezelRadius.ts`: `getResponsiveBezelRadius(contentWidth, multiplier)` — same ratio-based-default pattern as `getResponsiveFontSize` (base ratio 0.18 matches the old hardcoded value exactly, so multiplier 1.0 is pixel-identical to before). Deliberately based on the frame's own `contentWidth` (already `width * 0.78`, itself proportional to composition width) rather than composition width/height directly — the radius is a property of the content window's own size, and this keeps it scaling correctly at both orientations without a second, redundant ratio constant.
- `FrameSettings` (`src/frames/types.ts`) gained `bezelRadiusMultiplier: number`; `MinimalBezel.tsx` takes a `bezelRadiusMultiplier` prop (default 1) and computes `contentRadius` via the new helper; `FrameRenderer.tsx` passes it through from `frameSettings`. Other frames (Square Bezel etc.) are untouched — this is Minimal-Bezel-only, per the request.
- `ProjectContext.tsx` got the full persisted-state/interface/useState/memo/writeToStorage-deps/provider-value plumbing (mirrors `fontSizeMultiplier`'s pattern exactly, 6 touchpoints).
- `OverlayFramesPage.tsx` (`/overlay/frames`) gained a "Corner Radius" slider, shown only when `frameVariant === 'minimalBezel'`, range 0%–150% step 5%, same UX (label + `<input type=range>` + `accent-primary`) as the Style tab's Font Size slider. Not added to the condensed Overlay-tab Frame card (that card only shows the variant grid, same as it never showed Shell Color either — full controls live in the "More frames" drill-in only).
- Verified via a real `bundle()` → `selectComposition()` → `renderStill()` pass (temporary gitignored `tmp/` script, deleted after verification) at both orientations and 3 multiplier values each (0, 1, 1.5) — cropped each render to the content window's top-left corner and visually confirmed: 0% is a sharp square corner, 100% (default) is clearly rounded and pixel-identical in appearance to the pre-change hardcoded radius, 150% is visibly more rounded; same relationship holds at both 1080x1920 and 1920x1080. `tsc -b`/`npm run build` both clean. New `LEARNINGS.md` entry: `npx ffmpeg` isn't runnable (ffmpeg isn't an npm-published CLI) — resolve the project's own `ffmpeg-static` dependency's binary path instead for any future verification script that needs ffmpeg.
- **Live-verified by the user in the browser** (Overlay → Frame tab, Minimal Bezel selected: Corner Radius slider rounds/sharpens the preview's phone-shell corners live). Session 13 fully done.

## Session summaries (condensed — see PLAN.md for full technical detail per part)

**Session 1-2 (faceless-app):** Full caption pipeline (SRT/interpolation → captions.json, processCaptions split/merge/contiguous-frames, signature style, MasterComposition, WaveBackground/LineGrid/registry, hook scenes, subject scenes, legibility scrim). All done, verified.

**Session 3 (Caption Studio scaffold, PLAN.md Part E):** Vite+React+TS+Tailwind v4 app, 5 routes, persistent shell, DESIGN.md tokens, local transcription server (own Whisper.cpp `small.en`, self-contained — not shared with faceless-app), Style tab live-wired, Green Screen + SRT exports real, Overlay tab (watermark/progress bar) real. Plus polish passes: project naming/autosave/real resolution scaling, aspect-ratio-toggle bugfixes. All done, verified.

**Session 4 (Caption customization, PLAN.md Part F):** Keyword highlighting decoupled from animation choice (shared `applyKeywordEmphasis` helper), glow made a real tunable `highlightIntensity`, original 8-preset font table added via `@remotion/google-fonts` (verified real typefaces render in the export path, not silent Arial fallback), 3 new animations built (Typewriter, Slide-up, Outline Draw-on — the last took 3 iterations, two-overlaid-text-nodes drift bug fixed via single-element `background-clip: text` gradient technique, see LEARNINGS.md), editable keyword list UI done. Font-per-category auto-suggest deferred, folded into Session 6 instead. All done, verified live.

**Session 5 (Reel Craft ports + transcript editing, PLAN.md Parts G/H) — DONE:**
- [x] G1. 4 frame components ported (Minimal Bezel, Gradient Border, Neon Glow, Cinematic Scope) — live-verified
- [x] G2. Frame selector wired into ProjectContext/PreviewPlayer/CaptionExportComposition — live-verified
- [x] G3. 3 texture overlays ported (Film Dust, Halation, Grid) — live-verified (Film Dust/Halation/Grid all confirmed live + in a real export)
- [x] G4. Frame+overlay+captions compositing verified live AND via real export
- [x] H1. TranscriptEditor.tsx read-only rendering — verified live (Playwright, 261-word transcript, scroll/wrap confirmed)
- [x] H2. Click-to-edit tokens — `TranscriptEditor.tsx` tokens editable, `ProjectContext.updateCaptionText` writes back into `captions` + keeps the localStorage transcript cache in sync; `tsc`/`build` clean; live-verified via H4
- [x] H4. Entry points — `TranscriptEditorModal.tsx` auto-opens post-transcription + persistent Sidebar "Transcript" trigger from any tab; **live-verified**, all 7 checklist items passed (auto-open, sidebar trigger from all tabs, click-to-edit Enter/Escape/blur, persistence through modal close/reopen and page reload)
- [x] H3. Empty-text deletion + processCaptions.ts filter-before-pipeline fix — **live-verified**, all 3 checklist items passed
- [x] H5. Final verify — **live-verified** via a real 4-minute Chroma Key export reflecting the user's transcript edits correctly

**Also shipped this session, not a PLAN.md phase:**
- Horizontal/16:9 orientation bug fixed and **live-verified** (Playwright + real export render at both orientations confirmed correct dimensions)
- Dev-workflow transcript caching (localStorage, not IndexedDB — captions-only, no media blob) — `tsc` clean, not live-verified
- PreviewPlayer remount-on-navigate fix — **live-verified** (playback survives Import/Style/Overlay/Export navigation, selection changes don't restart it, Import empty state works from both dropzone and panel button)
- Nested routing restructure (Style/Overlay condensed + sub-routes, `CondensedCard`/`SubPanelView` reusable pattern) — **live-verified** (condensed cards, "More X" drill-in/back, selection persistence, Minimal Bezel shell color + Halation/Grid intensity pickers all confirmed)
- Position control simplified to Top/Center/Bottom with safe-margin inset — **live-verified**, including real Green Screen export at both orientations
- Responsive font size (height-based) + Cinematic Scope content-inset fix + z-order fix — **live-verified**, including real Green Screen export of Cinematic Scope + Bottom position

## Known non-bugs / accepted gaps
- TopBar Help button, ImportPage history icon — intentionally non-functional, no content to wire yet
- `npx tsc --noEmit`'s pre-existing `CalculateMetadataFunction<Props>` type error — predates Session 3, not fixed, cosmetic
- Reported keyword-overlap bug ("PUT THOSE TOGETHER") — could not reproduce against the fontSize-based emphasis fix in any variation tried; likely stale HMR/cache. Re-test with a hard refresh if it recurs.

## Session 6 — Orientation-aware fonts + global legibility (PLAN.md Part I)
- [x] I1. Trimmed Editorial (Source Sans 3) and Rounded Friendly (Poppins) as redundant; added all 12 researched presets (Meme Energy/Anton, Screenplay/Courier Prime, Playful Comic/Bangers, Business Bold/League Spartan, Handwritten/Caveat, Cinematic/Jost, Elegant Serif/Cinzel, Essay Slab/Arvo, Calm Organic/Quicksand, Accessible/Atkinson Hyperlegible, Sci-Fi Tech/Rajdhani, Luxury Display/Marcellus) — all 12 verified as real `@remotion/google-fonts` entries against the installed package's font list before wiring, no substitutes needed. 18 total presets now in `src/captions/styles/presets.ts`, each tagged with `orientation` (`vertical`/`horizontal`/`both`) and `suggestedAnimation`. Verified via `tsc`/`build` clean AND a real `bundle()` → `selectComposition()` → `renderStill()` pass through the actual export composition (the real risk path per LEARNINGS.md — loadFont() only gets exercised through the Remotion bundler, not plain `vite build`) — all 18 fonts' `loadFont()` calls resolved with no errors. Not yet live-verified in the browser Style tab UI (I3 will wire the condensed/full-list filtering that surfaces these); orientation/suggestedAnimation fields exist on the data but aren't consumed by StylePage yet.
- [x] I2. Added `src/captions/styles/legibility.ts` (`getLegibilityStroke`/`getLegibilityShadow`/`combineTextShadow`), both ratios computed off `getResponsiveFontSize` so they hold at both orientations. Signature/CalmPhrase/Typewriter/SlideUp: replaced their old fixed `WebkitTextStroke: "3px black"` with the font-size-scaled stroke; all 5 animations (incl. OutlineDraw) now combine the legibility drop shadow with whatever keyword-emphasis glow `applyKeywordEmphasis` produces via `combineTextShadow` (CSS text-shadow accepts a comma list) instead of one clobbering the other. OutlineDraw's own white 2px outline was left untouched — that's the animation's core visual effect (a real stroke, not a legibility one), only the drop shadow was layered on top of it. Verified via a real `bundle()` → `selectComposition()` → `renderStill()` pass through the export composition for all 5 animations, visually confirmed (dark stroke + soft halo visible behind text, keyword glow still layers correctly on top) — not just tsc/build clean.
- [x] I3. `presets.ts` gained `getPrioritizedFontPresets(activeOrientation)` — reorders `FONT_PRESETS` so the active orientation's own presets lead (in table order), then 'both' presets, then the rest, so a `slice(0, CONDENSED_FONT_COUNT)` off it surfaces the Part I research picks first instead of whichever 'both' preset happened to sit earliest. `StylePage.tsx`'s condensed Font card now reads `useLayout().layoutMode` and slices this reordered list instead of the raw `FONT_PRESETS` array. `StyleFontsPage.tsx` (`/style/fonts`) gained an All/Vertical/Horizontal/Both segmented filter (local `useState`, defaults to All so nothing's hidden by default when arriving via "More fonts"). `tsc`/`build` clean; not yet live-verified in the browser (I4 covers that).
- [x] I4. Live-verified by the user: condensed picks change correctly by orientation, full-list filter works, and stroke/shadow legibility holds up against real frames + texture overlays at both orientations. User flagged that fonts aren't restricted by orientation (a vertical-tagged font can still be picked while in horizontal mode, and vice versa) — deliberate, matches the existing "hint not constraint" pattern already used for `suggestedAnimation` (Part F); decided to leave as-is rather than add a hard restriction.