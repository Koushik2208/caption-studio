# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Whisper.cpp binary + models (machine-specific, re-download via scripts/setupWhisper.mjs)
tools/whisper
tmp

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local state (machine-specific)
.claude/scheduled_tasks.lock

# Env files (none currently used, but keep local secrets out if added later)
.env
.env.local
.env.*.local
